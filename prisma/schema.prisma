// Этот файл оставлен для совместимости со сборкой
// Проект использует Supabase вместо Prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
} 